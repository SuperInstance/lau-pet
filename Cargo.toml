[package]
license = "MIT"
name = "lau-pet"
version = "0.1.0"
edition = "2021"
description = "Companion pet system for Lau — each kid gets a pet that grows and evolves based on their learning."

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0.150"
